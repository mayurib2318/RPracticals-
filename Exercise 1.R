# 1. First 15 rows of mtcars
head(mtcars, 15)

# 2. Maximum and minimum Sepal.Length from iris
max(iris$Sepal.Length)
min(iris$Sepal.Length)

# 3. Mean of mpg in mtcars
mean(mtcars$mpg)

# 4. Structure of airquality dataset
str(airquality)

# 5. Check if 100 is greater than 50
100 > 50

